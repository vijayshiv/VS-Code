// This is called the header file // '#' is called preprocessor directive
// stdio means standard input output // '.h ' is used for header file extension
#include <stdio.h>
#include <conio.h>

int main() {
    // char ch;
    // ch = 256;
    // printf("%d \n", ch);
    printf("Hello world");
    return 0;
}